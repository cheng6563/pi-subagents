# Pi 通用 subagents 执行器

为 Pi 提供 `subagent` 工具和 `/subagents` 运行面板，将任务交给独立子代理执行，支持进度查看、结果回收、暂停与恢复。

不内置业务角色或审核流程；任务要求由调用方提供。支持指定模型、附加 Markdown 要求文件和受限的多层派生。

基于原 pi-subagents 裁剪后独立维护，不常规同步上游。保留 MIT 许可及历史归属。
