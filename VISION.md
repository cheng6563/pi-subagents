# 维护目标

本项目独立维护一个通用 Pi 子代理执行器，不常规同步上游。复用适用的运行时代码，按需评估具体修复，保留原许可与历史归属。

执行器只负责确定性任务交接、要求文件快照、模型选择、派生额度、上下文边界、工具与扩展加载、运行状态、取消、恢复和结果回收。任务方法、角色、业务标准、审核和验收由调用方的本次任务及显式附加的要求文件定义。

默认使用父模型、fresh 对话上下文和一层派生额度。失败不自动换模型、不扩大权限、不重放可能已发生的副作用。正常结束仅表示运行完成，不代替业务验收。

不维护旧角色、旧工作流和旧策略 API 的兼容路径。工具和文档应描述实际可用的接口。派生额度只控制 subagent 工具的可用性，不限制其他工具和扩展。
