# 维护目标

本项目独立维护一个通用 Pi subagents 执行器，不常规同步上游。复用适用的运行时代码，按需评估具体修复，保留原许可与历史归属。

执行器只负责确定性任务交接、要求文件快照、模型选择、派生深度、上下文边界、工具与扩展加载、运行状态、取消、恢复和结果回收。任务方法、角色、业务标准、审核和验收由调用方的本次任务及显式附加的要求文件定义。

默认使用父模型、fresh 对话上下文，最多派生一层。失败不自动换模型、不扩大权限、不重放可能已发生的副作用。正常结束仅表示运行完成，不代替业务验收。

不维护旧角色、旧工作流和旧策略 API 的兼容路径。工具和文档应描述实际可用的接口。派生深度限制只控制 subagent 工具的可用性。子会话另行排除父会话负责的任务清单、交互提问和持久记忆修改工具及其摘要／指南，禁用 task-list 生命周期钩子；其余执行工具和记忆读取能力正常保留。
